// Return a pyramid (See genius provided answer below...)
let row = "";
for(let i = 0; i < 7; i++){
  row += "#";
  console.log(row);
}

// PROVDED ANSWER
// for (let line = "#"; line.length < 8; line += "#")
//   console.log(line);
