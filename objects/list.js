// Return a list object, from an array
const arrayToList = (arr) => {
  let list;

  return list;
}

// Return an array, from a list object
const listToArray = (list) => {
  let arr;

  return arr;
}

// Prepend an item to a list
const prepend = (item, list) => {
  let newList;

  return newList;
}

// Return the nth item of a list object
const nth = (list) => {
  let nth;

  return nth;
}